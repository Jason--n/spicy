# @TEST-REQUIRES: have-zeek-plugin
#
# @TEST-EXEC: spicyz -o dhcp.hlto ${DIST}/spicy/lib/protocols/dhcp.spicy ${DIST}/zeek/plugin/lib/protocols/dhcp.evt ${DIST}/zeek/plugin/lib/protocols/zeek_dhcp.spicy
# @TEST-EXEC: ${SCRIPTS}/run-zeek -NN dhcp.hlto | grep -q spicy_DHCP
# @TEST-EXEC: ${SCRIPTS}/run-zeek -C -r ${TRACES}/dhcp.pcap dhcp.hlto > output 2>&1
# @TEST-EXEC: false
# @_TEST-EXEC: ${SCRIPTS}/run-zeek -r ${TRACES}/dhcp.pcap dhcp.hlto ${DIST}/zeek/plugin/lib/protocols/tftp.zeek && cat conn.log | sort >conn.rrq.log && mv tftp.log tftp.rrq.log
# @_TEST-EXEC: ${SCRIPTS}/run-zeek -r ${TRACES}/tftp_wrq.pcap tftp.hlto ${DIST}/zeek/plugin/lib/protocols/tftp.zeek && cat conn.log | sort >conn.wrq.log && mv tftp.log tftp.wrq.log
# @_TEST-EXEC: TEST_DIFF_CANONIFIER=${SCRIPTS}/canonify-zeek-log btest-diff conn.rrq.log
# @_TEST-EXEC: TEST_DIFF_CANONIFIER=${SCRIPTS}/canonify-zeek-log btest-diff tftp.rrq.log
# @_TEST-EXEC: TEST_DIFF_CANONIFIER=${SCRIPTS}/canonify-zeek-log btest-diff conn.wrq.log
# @_TEST-EXEC: TEST_DIFF_CANONIFIER=${SCRIPTS}/canonify-zeek-log btest-diff tftp.wrq.log
