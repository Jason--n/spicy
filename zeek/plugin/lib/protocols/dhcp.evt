protocol analyzer spicy::DHCP over UDP:
    parse with DHCP::Message,
    port 67/udp,
    port 68/udp,
    replaces DHCP;

import Zeek_DHCP;

on DHCP::Message -> event DHCP::log_dhcp($conn, $is_orig, Zeek_DHCP::create_msg(self));
